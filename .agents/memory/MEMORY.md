- [outdoor-today AI system](outdoor-today.md) — homepage "Best outdoor experiences"; system_key='outdoor_today'; day-scoped 10-min cache; MUST never render empty (fall back to all trips when no today-slots).
- [TourCMS API logging & rate limit](tourcms-api-logging.md) — all outbound TourCMS calls logged to error-log source 'tourcms'; rate_limit_status.xml excluded (free, doesn't count against quota); GET/POST limits surfaced on /admin/palisis.
- [Anthropic key resolution](anthropic-key-resolution.md) — every AI route must read Anthropic key DB-first (integrations.anthropic) then env; env ANTHROPIC_API_KEY is stale/invalid, env-first causes intermittent 401s (itinerary route was the outlier).
- [AI provider central resolver](ai-provider-resolver.md) — every AI feature routes through resolveAi (lib/ai/provider.ts); admin picks Anthropic/OpenAI; provider switch auto-remaps stored models by tier; blog DALL·E stays OpenAI-only.
- [Itinerary hybrid engine](itinerary-hybrid-engine.md) — AI selects trips, deterministic scheduler times them; gap=travel+buffer+earlyArrival (separate); max-5 cap; no-early opt-in only; AI key often 401.
- [Planner two trip stores](planner-two-stores.md) — working "My Trip" list (planner-list-context, drives itinerary) vs saved library (cart-context, bookmark); all build paths + fingerprint must exclude date-disabled trips identically.
- [Frontend Edit Mode editor](edit-mode-editor.md) — `?admin_edit=1` editor: explicit EditableText/Image wrappers + generic AutoEditableLayer; exclude via data-editable/data-no-edit; page-content store is in-memory (not durable, edit-mode-only).
- [Header/Footer injection](header-footer-rendering.md) — admin header_footer_blocks render via layout.tsx + CustomHtmlBlock client injector that re-creates <script> tags (dangerouslySetInnerHTML won't run scripts); head blocks land in body, not <head>.
- [Itinerary availability parity](itinerary-availability-parity.md) — bulk datesndeals under-reports vs authoritative real-time checkavail; always fall back to checkavail when visit date has 0 bulk slots; GET-only retries; bounded fan-out; failed call=TOURCMS_ERROR not NO_SLOTS.
- [Planner pref update paths](planner-pref-update-paths.md) — a planner pref can change via 4 separate paths (onboarding steppers, applyDirectPref, onToolCall chat merge, route schema); cap/rule changes must touch all four.
- [Help page data](help-page-data.md) — /help reads DB (not hardcoded FAQ_DATA); articles hidden behind category grid so test via search box; dup root cause = un-guarded re-run seeds, now NOT EXISTS guarded.
- [File-upload rules & Help attachments](file-upload-attachments.md) — effective rules user??global??DEFAULT, SAFE_EXTENSIONS backstop; help upload needs own help-gated endpoint (proxy RBAC is path-prefix); sanitize attachment URLs (stored XSS); proxyClientMaxBodySize for >10MB.
- [SEO indexing control](seo-indexing-control.md) — same codebase on Replit-publish (staging) + live domain; indexing opt-in via ALLOW_INDEXING; no static public/robots.txt (conflicts with app/robots.ts).
- [Error logging + key tests](error-logging-and-key-tests.md) — error_logs table via fail-soft logError (void it); admin Test-API-key route must POST keys in body, never URL query (leak risk).
- [Planner chat UI gotchas](planner-chat-ui.md) — handleSend must allow "error" status (else chat freezes after AI fail); recs duration cap lives in fallbackTrips via durationCapHours+fitsCap.
- [Planner recommendations canvas](planner-recommendations.md) — canvas recs are deterministic & decoupled from AI chat (fixes reload hang); date=visual grouping only; availability fetch must clear on each scan; pills edit one field via applyDirectPref.
- [Integration key precedence](integration-key-precedence.md) — admin-panel DB keys are source of truth; env is fallback only. Applies to Mapbox/TourCMS/OpenWeather/Google/Anthropic.
- [TourCMS MULTI tours](tourcms-multi-tours.md) — start_time="MULTI" tours have bookable dates w/ no time; resolve real slots via checkAvailability, not datesndeals.
- [Planner preflight card state](planner-preflight-state.md) — preflightCardState volatile (resets on refresh); restored buildItinerary cards must be rehydrated to "ready"; only latest card renders full.
- [Itinerary list↔map sync](itinerary-map-sync.md) — shared active indices are full-step space; map renders a subset & skips unresolved steps, so a stepIndices array translates at the map boundary.
- [Deploy build force-dynamic](deploy-build-force-dynamic.md) — DB-backed server page.tsx on static routes MUST be force-dynamic; else deploy `next build` prerenders & fails w/ "Connection terminated" (no DB in build sandbox). Local build passes (DB reachable).
- [Media library](media-library.md) — admin Files lib (`/admin/files`, perm `files`, table media_files); blob-or-local upload; SVG/active-content MIME excluded (XSS).
- [Planner cross-surface invariants](planner-cross-surface-invariants.md) — resolvePalisisId exists in BOTH planner+itinerary routes (keep fail-closed in sync); multi-day hide spans 5 surfaces.
- [Itinerary PDF export & data shapes](planner-itinerary-pdf-export.md) — chat-card buildItinerary output is a LIGHTER Itinerary (no lat/lng/tips/prices) than canvas centerItinerary; full-data features (PDF/map) must use centerItinerary; ItineraryStep not exported.
- [Media upload pipeline](media-upload-pipeline.md) — every admin upload MUST route through lib/media-upload.ts (processUpload/processUploadFile); dedup by sha256 content_hash w/ unique partial index + ON CONFLICT; careers CV upload is public, excluded. Also: Palisis trip-image URL-import (processImageFromUrl, source_url dedup, SSRF guard — required) + backfill.
- [Activity audit log](activity-audit-log.md) — admin mutations call fire-and-forget logActivity(actor=session) after write; DELETE & non-CRUD handlers easily missed; activity surface superadmin-only; e2e via minted jose JWT cookie.
- [Admin RBAC](admin-rbac.md) — employee accounts w/ per-section perms; proxy gates via STALE JWT (edge), requireAdminSession is DB-fresh; every new /api/admin/* endpoint MUST be added to ROUTE_RULES or employees get false 403.
- [Planner chat transport context](planner-chat-transport-context.md) — transport created once ([] deps); prefs + My Trip list MUST be ref-backed (synced in effect + at mutation time) or AI sees stale/empty cart; route RULE 11 makes list+date authoritative.
- [Admin mobile drawer](admin-mobile-drawer.md) — admin sidebar is an overlay drawer on mobile; gate collapse via effectiveCollapsed=isDesktop&&collapsed so desktop collapse never leaks into the mobile drawer.
- [Itinerary map](itinerary-map.md) — trip-detail map + OPTIONAL per-step lat/lng/placeName in JSONB; legend renders only when a step has coords; headless test browser has NO WebGL so verify map via geocoding+DOM+graceful fallback, never the canvas.
- [Next16 redirect streaming](next16-redirect-streaming.md) — server-component redirect() degrades to soft (chunked-200) under streaming; do SEO/canonical redirects in proxy.ts (real 308) via loopback-fetch to a tiny DB endpoint, never public-host self-fetch.
- [SEO score staleness](seo-score-staleness.md) — score = effective seo_* fields (frozen on optimize), NOT base content; staleness (seo_source_hashes) is the "outdated" signal; trip PATCH auto-recomputes seo_score via liveScoreForTrip but never refreshes the hash snapshot.
- [Data Migrations system](data-migrations-system.md) — versioned DATA-only (never DDL) content sync dev→live via /admin/db-migrations (superadmin); schema still goes through Publish; idempotent + 42P01 fallback.
- [Markdown link URLs + blog trip slugs](markdown-link-urls.md) — markdown inline() must shield link hrefs from emphasis (else /trip/tcms_5 mangles to /trip/tcms<em>5); blog rewrites /trip links→slug BEFORE sanitize.
- [Blog publish scheduling & draft visibility](blog-publish-scheduling.md) — public gate (published & published_at<=NOW) lives in DB POST_PUBLIC_GATE + mirrored isPostLive; route ALL public reads through dbListPublicPosts/dbGetPostBySlug; admins preview non-live via /blog/[slug]; notFound() may 200 under streaming (content still hidden).
- [Announcement banner](announcement-banner.md) — structured banner = integrations key='announcement' (value=sanitized HTML, meta{enabled,size}); legacy 'announcement_banner' row MUST be excluded from BOTH injection merge paths or it re-renders.
- [SEO column ownership](seo-column-ownership.md) — seo_* trip columns are written ONLY by the /seo route; the general trip PATCH must STRIP them or the form-save spread (stale null seo_*) wipes accepted SEO.
- [Planner prompt storage](planner-prompt-storage.md) — planner override now on planner.system_prompt (not chat.extra); dual read fallback to legacy in admin+route must stay in lockstep; form stays in chat.extra.planner.form; migration 006 relocates.
- [SSE stream hardening](sse-stream-hardening.md) — AI SSE routes need keepalive heartbeat (proxy idle 502), cancel()+guarded emit (client-disconnect "Controller closed" spurious logs), body-parse try, outer setup try.
- [AI error logging coverage](sse-stream-hardening.md) — all AI routes log via lib/error-log requestMeta(req) {method,path,referer,userAgent}; streaming routes log via streamText onError AND setup catch; surfaced at /admin/logs (superadmin+devOnly nav).
